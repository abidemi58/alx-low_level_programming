#include "main.h"
/**
 * add - adds two integers and returns the result.
 * @n: the number that will be added
 * @m: the number to be added
 * Return: the result of the sum
 */
int add(int m, int n)
{
	int o;

	o = m + n;
	return (o);

}
