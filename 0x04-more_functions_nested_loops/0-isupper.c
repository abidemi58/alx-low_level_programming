#include "main.h"
/**
 * _isupper - checks for uppercase character.
 * @c: the chareacter to be checked
 * Returns 1 if c is uppercase
 * Returns 0 otherwise
 */
int _isupper(int c)
{
	char c;

	if (c = 'A'; C <= 'Z')
		return (1);
	else 
		return (0);
}
